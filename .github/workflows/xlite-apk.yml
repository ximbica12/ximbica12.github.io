name: Build XLite APK

on:
  push:
    branches:
      - xlite-apk-build
    paths:
      - 'xlite-build/**'
      - '.github/workflows/xlite-apk.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Ensure Android SDK 35
        run: |
          SDKMANAGER="$(find "$ANDROID_SDK_ROOT/cmdline-tools" -type f -name sdkmanager | head -n 1)"
          if [ -n "$SDKMANAGER" ]; then
            yes | "$SDKMANAGER" --licenses >/dev/null || true
            "$SDKMANAGER" "platforms;android-35" "build-tools;35.0.0"
          else
            test -f "$ANDROID_SDK_ROOT/platforms/android-35/android.jar"
            test -x "$ANDROID_SDK_ROOT/build-tools/35.0.0/aapt2"
          fi

      - name: Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        working-directory: xlite-build
        run: gradle --no-daemon --stacktrace :app:assembleDebug

      - name: Rename APK
        run: cp xlite-build/app/build/outputs/apk/debug/app-debug.apk XLite-for-X-Android8-ARMv7-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: XLite-for-X-Android8-ARMv7
          path: XLite-for-X-Android8-ARMv7-debug.apk
          if-no-files-found: error
